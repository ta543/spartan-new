# Font Assets

Place self-hosted font files for this project in this folder.

Recommended structure:
- `public/fonts/inter/`
- `public/fonts/montserrat/`

The current global `@font-face` rules still reference remote font files, but this folder now exists as the canonical home for any local font assets added later.
